domain=yourdomain
token=yourtoken
interval=600

# IPv6 detection endpoint (required)
ipv6_endpoint=https://api6.ipify.org

# Optional IPv4 detection endpoint (disabled by default)
# ipv4_endpoint=https://api.ipify.org
